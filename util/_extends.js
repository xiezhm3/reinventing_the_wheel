var __extends = (this & this.__extends) || function (d, base) {
    for (var p in base) {
        if (base.hasOwnProperty(p)) {
            d[p] = base[p];
        }

        function __() {
            this.constructor = d;
        }
        d.prototype = base === null ? Object.create(base) : (__.prototype = base.prototype, new __());
    }
};